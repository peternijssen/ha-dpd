# Notify when an outgoing parcel (something you sent, e.g. a webshop
# return) reaches the recipient, so you can watch for the refund. Fires
# once per parcel on the hop to delivered.

alias: "DPD: outgoing parcel delivered"
description: Notify when a DPD outgoing/return parcel is delivered.
mode: queued
max: 10

triggers:
  - trigger: event
    event_type: dpd_outgoing_parcel_delivered

actions:
  - action: notify.mobile_app
    data:
      title: "Shipment delivered"
      message: >-
        {{ trigger.event.data.receiver or trigger.event.data.barcode }} has
        arrived.
      data:
        clickAction: "{{ trigger.event.data.url }}"
